import React from 'react';
import { SearchMainFormDesc } from './styled';


const SearchDescription = ({ text }) => {
	return (
		<>
			<SearchMainFormDesc text={text} />
		</>
	);
}

export default SearchDescription;